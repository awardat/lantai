# 兰台 v0.1.50 代码与文档综合审核报告

| 项目 | 内容 |
|------|------|
| 评审对象 | 兰台（lantai）本地 RAG 知识库 v0.1.50（含桌面壳 lantai-shell） |
| 评审范围 | backend/app/ 全部源码（含 0.1.39 混合检索/FTS5/R106 rerank、0.1.41 查询改写、0.1.43/44 表格 NL、0.1.45 reparse、0.1.46 本地 OCR、0.1.47 去噪去重、0.1.49 分页/孤儿清理）、frontend/（rerank 槽位/分页/轮询修复）、shell/、scripts/、docs/ 全部文档；回归验证 v0.1.38 审核 M1 与中间 4 份审核报告（v0.1.39/43/44/46）整改；评估 0.1.39~0.1.50 十二个版本实现质量 |
| 评审方式 | 逐文件阅读 + 跨模块交叉校验 + grep 精确核验；检索链路架构级变更逐行复核 |
| 评审时机 | **发版前审核**（当前版本号 0.1.50 为上一已发布版本，0.1.51 发布前审核；版本号标记滞后属正常状态，不计为问题） |
| 生成时间 | 2026-08-25 |
| 结论 | v0.1.38 审核 M1 + 中间 4 份审核报告整改**全部落地**；0.1.39~0.1.50 十二个版本实现规范，**0 代码 bug**。本次新发现 **0 项高危/严重/中危、1 项低危**。含 shell 全量 0 代码 bug；**文档同步质量达到历史最佳**——CH-087 分类表硬性检查点根治了 recurring 滞后问题 |

---

## 一、回归验证

### 1.1 v0.1.38 审核整改（CH-074）与中间审核报告

| 来源 | 编号 | 问题 | 状态 | 证据 |
|------|------|------|:---:|------|
| v0.1.38 | M1 | 技术对接方案 :153"整页渲染列入评估"与已引入 pymupdf 矛盾 | ✅ | `技术对接方案.md:338` 改"**整页渲染已落地为兜底**（0.1.38，CH-070/R110）"；§4.3 补三行机制 |
| v0.1.39 审核 | CH-076 M1/M2/L1 | FTS 清理顺序/score 量纲/维度静默降级 | ✅ | `store.py:263-267` `_purge_chunks` 先清 FTS 再删主表（唯一出口）；`keyword_search` sigmoid 归一 0~1；`retriever.py:92-101` 维度不一致上抛不吞 |
| v0.1.43 审核 | CH-087 M1/L1/L2 | 分类表漏表格 NL 机制 | ✅ | `技术对接方案.md:146` 补表格 NL 行；CH 登记必答"分类表受影响"升格为 AGENTS 自测清单第 9 项硬性检查点 |
| v0.1.44 审核 | CH-088 | kv 丢末列/回退名误接续 | ✅ | TC-149 边界用例覆盖 |
| v0.1.46 审核 | — | （审核报告存在，整改并入各版） | ✅ | §4.3 pdf_image 行含本地 Tesseract/噪声清洗 |

**回归结论**：全部整改落地。**CH-087"分类表核对"硬性检查点是关键流程升级**——0.1.43 起 CH 登记必答"该变动是否影响 §4.3 分类表"，根治了自 v0.1.16 以来连续多轮的文档滞后 recurring 问题。

---

## 二、0.1.39~0.1.50 十二个版本实现质量

| 版本 | 功能 | 实现评估 |
|------|------|----------|
| 0.1.39 | 混合检索 R107 + rerank R106 + embedding 降级 | ✅ `retriever.py` 架构清晰：向量∪BM25 → RRF 融合 → 可选精排；`store.py:116` FTS5 + bigram（schema v3 旧库回填 :135-142）；`keyword_search` sigmoid 归一；降级仅 embedding 故障（维度不一致上抛不吞，CH-076/L1） |
| 0.1.40 | rerank 候选池提前截断修复（CH-078）+ 横幅（CH-077） | ✅ `retriever.py:18` RECALL_TOP=20 固定候选池（rerank 前不截断）；`run.py:37-55` 通配地址拆本机/局域网提示 |
| 0.1.41 | 查询改写（CH-079）+ rerank 日志（CH-080） | ✅ `retriever.py:29-51` `_rewrite_query`（chat 槽位、失败回退原问题、slot=query_rewrite）；`llm.py:208-253` rerank 成功/失败均写 agent_log |
| 0.1.42 | 改写稳定化 + 召回扩宽（CH-081） | ✅ `retriever.py:41` 改写强制 temperature=0.0；`config.py:60` DEFAULT_TOP_K 5→8 + 前端同步（app.js:522 top_k:8） |
| 0.1.43 | Office 表格转 NL（CH-084/087） | ✅ `filetype._table_to_nl`（首行表头、"表名：表头为值"，纯规则零 LLM）接入四类解析；xlsx number_format / xls ctype 格式转换 |
| 0.1.44 | 电子 PDF 表格结构化（CH-085/088） | ✅ `filetype.pdf_extract_tables_nl`（pdfplumber 双策略/跨页接续/键值-列式区分）；requirements 补 pdfplumber>=0.11.10 |
| 0.1.45 | 文档重新解析（CH-089） | ✅ `docs.py:157-167` POST reparse（parsing 拒绝/其余清切片重入队）；`store.reparse_document`；前端 ready 行按钮 + `scripts/reparse_all.py` |
| 0.1.46 | 本地 OCR Tesseract（CH-090） | ✅ `pipeline._ocr_image_tesseract/_run_tesseract`（chi_sim+eng、PATH/常见路径/TESSDATA_PREFIX 探测）；`schemas.AiItem.local_ocr` 开关；前端 pdf_image 卡开关；README 安装指引 |
| 0.1.47 | OCR 噪声清洗 + 去重（CH-092/091） | ✅ `pipeline._scrub_ocr_noise`（LaTeX/纯符号/连续同字符/重复行过滤，清洗空页不入库）；同文档切片精确去重；坏页逐页 try 容错 |
| 0.1.48 | 本地 OCR 进 agent 日志（CH-093） | ✅ `_ocr_image_tesseract` 写 slot=ocr_local；`_scrub_ocr_noise` 正则误杀修复（`(.)\1{5,}`→`([^ \t])\1{5,}`） |
| 0.1.49 | 删除一致性 + 孤儿清理 + 分页（CH-094） | ✅ `store._purge_chunks` 唯一删除出口（先 FTS 后主表）；`gc_orphan_chunks` 启动清理；`list_documents` 分页+stats（无参兼容）；前端翻页栏 |
| 0.1.50 | 轮询覆盖修复（CH-097） | ✅ `app.js:188-218` 轮询复用 loadDocs（同走分页/筛选），停止判断用全量 lastStats——翻页/筛选不再被覆盖回全量 |

**结论：十二个版本实现规范，0 代码 bug。**

检索链路架构（本次重点逐行复核）：
- **流水线分层清晰**：改写 → 向量召回（故障降级）→ BM25 → RRF 融合 → 可选精排，对外 `retrieve()` 接口不变，业务调用无感
- **RRF 融合实现正确**：单路退化（vec/bm 空）直接取单路；两路按排名倒数加分合并，无需调权重
- **候选池设计合理**（CH-078 教训）：RECALL_TOP=20 固定候选池，rerank 前不截断——修复"法律文档被 RRF 提前挤掉、rerank 鞭长莫及"
- **降级边界精确**：仅 embedding 网络故障降级 BM25；维度不一致等存储层问题上抛保留中文自救指引（不静默）
- **FTS 一致性保障**：add/clear/delete/reparse 四路径均同步 FTS；_purge_chunks 唯一删除出口（先 FTS 后主表，顺序颠倒会子查询恒空）；gc_orphan_chunks 启动兜底（实测 2 万孤儿→0）

---

## 三、本次新发现问题

### 🟠 高危 / 🔴 严重 / 🟡 中危

**无**。本次未发现任何高危、严重或中危问题——十二个版本实现规范，**含 shell 工程在内全量 0 代码 bug**。

---

### 🟢 低危

**L1. 技术对接方案 §7.8（R110 专用 OCR 引擎）未标"部分落地"口径**
`技术对接方案.md:267` 仍写"评估 PaddleOCR/piXEl 等本地 OCR…"，未更新为"Tesseract 已落地（0.1.46，§4.3）；PaddleOCR 仍评估"。§4.3 :149 已正确记录 Tesseract，仅档位3扩展点章节的 R110 条目未同步部分落地状态。与 0.1.38 M1 教训（"评估/备选"表述 vs 已落地）同类但程度更轻（§4.3 主表已正确）。

**L2. 前端 onclick 内联 + esc 不转义引号 / SSE 占线程池 worker（v0.1.11 L6/L7 延后项沿用）**
历史延后项，沿用。

---

### ℹ️ 提示（非问题）

- **文档同步质量达到历史最佳**：12 个版本的机制全部反映——技术对接方案 §2 技术栈（pdfplumber）/§4.3 分类表（表格 NL×2 行、OCR 噪声清洗、本地 Tesseract、整页渲染已落地）/§7.3（rerank/hybrid 已实现）；API 说明文档 V1.47（分页/reparse/score 统一 0~1/rerank 槽位/混合检索说明）；测试方案 V1.22（TC-137~154 全覆盖 18 项新功能）；README（功能一览混合检索/改写/重排/表格 NL/reparse/分页、模型推荐补 rerank、本地 OCR 安装指引）。**CH-087 分类表硬性检查点根治了自 v0.1.16 以来 8 轮的文档滞后 recurring 问题**。
- **score 契约统一**：向量余弦/BM25 sigmoid/rerank 相关性分三来源统一 0~1（CH-076/M2 方案 A），外部协议（Dify score_threshold）语义一致。
- **测试方案 TC-140~154 质量高**：含一致性断言（chunks_fts == chunks）、回归断言（孤儿累积、0.0.0.0 横幅、正则误杀）、边界用例（3 列末列、英文跨行、节标题接续）。
- **版本号主同步 ✅**：config.py（0.1.50）、README（0.1.50）、测试方案（V1.22/0.1.50）、requirements 注释（v0.1.50）一致；API 说明文档 V1.47/0.1.49 滞后 1 版属发版前正常状态。
- **sample/ 新增投研报告/等级保护调查表/GBT 5271.18 样本**：表格 NL、PDF 表格结构化、OCR 去噪的回归基准。

---

## 四、优点

1. **检索链路架构升级规范**：hybrid+RRF+可选 rerank+查询改写四层增强分层清晰，对外接口不变，降级边界精确（网络故障降级 vs 存储问题上抛）。
2. **FTS 一致性设计成熟**：唯一删除出口 + 顺序保证 + 启动 GC 兜底，四路径全覆盖；实测 2 万孤儿清零。
3. **候选池教训快速吸收**：CH-078 用户实测发现问题 → RECALL_TOP=20 固定候选池修复 + TC-141 回归断言。
4. **CH-087 分类表硬性检查点根治 recurring**：8 轮文档滞后问题经流程升级（登记必答）后本轮 12 版零漏网——质量流程进化的标志性成果。
5. **测试用例质量高**：一致性/回归/边界断言兼备，TC-140/152/154 尤佳。
6. **含 shell 全量 0 代码 bug**：连续 29 个版本（0.1.22~0.1.50）0 代码 bug。

---

## 五、修复优先级建议

| 优先级 | 编号 | 建议动作 |
|--------|------|----------|
| 顺手修（发版时） | L1 | 技术对接方案 §7.8 R110 行补"Tesseract 已落地（0.1.46）；PaddleOCR 仍评估" |
| 可延后 | L2 | onclick/SSE 沿用 |

---

## 六、质量趋势

| 版本 | 严重 | 高 | 中 | 低 | 代码 bug | 备注 |
|------|:---:|:---:|:---:|:---:|:---:|------|
| v0.1.1 | 1 | 1 | 3 | 9 | 是 | 首次评审 |
| v0.1.21 | 0 | 2 | 10 | 18 | 是 | shell 引入 bug |
| v0.1.27 | 0 | 0 | 3 | 6 | 否 | |
| v0.1.31 | 0 | 0 | 0 | 4 | 否 | 发版前审核 |
| v0.1.38 | 0 | 0 | 1 | 2 | 否 | 1 中危文档矛盾 |
| v0.1.50 | **0** | **0** | **0** | **2** | **否** | **12 版 0 bug，文档同步历史最佳** |

**趋势**：连续 29 个版本 0 代码 bug；本轮 12 个版本（检索架构升级+6 项新功能）实现规范且文档全同步，仅 1 项低危（扩展点章节口径）。**CH-087 流程升级根治文档滞后 recurring 问题**——从"每轮 3~4 项滞后"到"12 版零漏网"，质量流程进化成效显著。

---

**文档状态**: 审核报告 v1.0（针对应用 v0.1.50 发版前审核，含桌面壳 lantai-shell）
**生成时间**: 2026-08-25
**前置报告**: docs/05-质量评审/代码与文档评审报告-v0.1.1.md ~ v0.1.46.md + Ox 系列
**说明**: 本报告仅审核，未修改任何被评审文件。发版前审核，版本号滞后属正常状态。
