# 测试方案：兰台（lantai）本地 RAG 知识库

| 项目 | 内容 |
|------|------|
| 测试对象 | 兰台 v0.1.17（Windows x64 单机部署） |
| 测试方式 | **人工测试**（手动执行用例 + 记录结果） |
| 测试范围 | 安装启动、界面导航、文档管理、问答、对话历史、配置、外部 API、错误容错、智能体日志、PDF 解析质量 |
| 生成时间 | 2026-08-23 |
| 前置文档 | PRD产品需求文档.md、API说明文档.md、版本记录.md |

---

## 一、测试环境准备

### 1.1 环境
| 项 | 要求 |
|----|------|
| 系统 | Windows 10/11 x64，浏览器 Chrome/Edge 最新 |
| 运行方式 | 二选一：源码运行（`cd backend && .\.venv\Scripts\activate && python -m uvicorn app.main:app --port 8000`）或编译版（`release/lantai-0.1.17-windows-x64\lantai.exe`） |
| 访问 | http://127.0.0.1:8000 |

### 1.2 AI 后端（按测试项选择）
| 方式 | 准备 |
|------|------|
| Mock AI（推荐默认） | `python scripts/mock_ai_server.py --port 18000`；设置页将 chat/embedding/文字类指向 `http://127.0.0.1:18000`（模型 mock-chat / mock-embed） |
| Ollama（可选） | 管理员安装 + `ollama pull qwen2.5:7b / bge-m3 / qwen2.5vl:7b` |
| 云端（可选） | 任一供应商 API Key |

### 1.3 测试数据
- `python scripts/make_sample_docs.py` 生成样例（txt/md/docx/pdf/图片）
- 样本目录 `sample/`：扫描件 PDF（`自 2026-06-18 04_10_08 PM 起扫描.pdf`）、Asperger 论文 PDF（56 页文字 PDF）
- 准备一个**超 20MB** 文件与一个 `.exe` 文件（负向用例）

---

## 二、测试用例

> 用例格式：**TC-ID｜前置｜步骤｜预期结果**。优先级：P0 核心链路 / P1 重要 / P2 细节。

### 2.1 安装与启动（P0）

| ID | 用例 | 步骤 | 预期 |
|----|------|------|------|
| TC-001 | 源码启动 | 按 1.1 启动 | 控制台输出 Uvicorn running，无报错 |
| TC-002 | 编译版启动 | 双击 lantai.exe | 浏览器自动打开 http://127.0.0.1:8000，首页正常 |
| TC-003 | 日志生成 | 启动后查看 `data/logs/` | 生成 `lantai-<时间戳>.log` 与 `agent-<时间戳>.log`，内容含启动信息 |
| TC-004 | 系统信息 | 设置→关于 | 版本号显示 **0.1.17**，平台/数据目录正确 |

### 2.2 界面与导航（P1）

| ID | 用例 | 步骤 | 预期 |
|----|------|------|------|
| TC-010 | 两栏布局 | 打开首页 | 左栏固定宽（logo + 会话历史 + 底部导航），右栏消息流 + 底部输入框 |
| TC-011 | 品牌显示 | 查看左栏顶部 | 品牌文字为繁体"**蘭臺**"，"兰"字方块 logo |
| TC-012 | 导航切换 | 点击底部"文件管理" | 右栏切换为文档管理视图；再切回"问答" |
| TC-013 | 设置入口 | 点击底部"设置" | 弹出**小窗口**密码门禁（无大留白） |

### 2.3 文档管理（P0）

| ID | 用例 | 步骤 | 预期 |
|----|------|------|------|
| TC-020 | 上传 txt/md | 上传样例 txt、md | 状态"解析中"→"已就绪"，切片数 > 0 |
| TC-021 | 上传 docx | 上传会议纪要样例.docx | 解析成功，切片数 > 0 |
| TC-022 | 上传文字 PDF | 上传产品手册样例.pdf | 解析成功 |
| TC-023 | 上传扫描件 PDF | 上传 sample/扫描件 PDF | **分类显示"图片 PDF（OCR）"**，OCR 通道处理后"已就绪"（Mock 下也 ready） |
| TC-024 | 上传大 PDF | 上传 Asperger 样本（56 页） | 解析成功，切片数百级，耗时可接受（≤1 分钟） |
| TC-025 | 上传图片 | 上传示例图片.png | 图片类解析（视觉描述）成功或跳过不阻塞 |
| TC-026 | 白名单拦截 | 上传 .exe 文件 | 拒绝并提示"不支持的文件类型"，中文提示 |
| TC-027 | 大小限制 | 上传 >20MB 文件 | 拒绝并提示超限 |
| TC-028 | 删除文档 | 删除任一文档 | 文档消失；再上传同名文件可重新入库 |
| TC-029 | 预览 txt/md | 点击预览 | 浮层显示原文 |
| TC-030 | 预览 docx | 点击预览 | 浮层显示提取文本 |
| TC-031 | 预览 PDF | 点击预览 | **浮层内浏览器原生渲染 PDF**（不触发下载），可翻页/缩放 |
| TC-032 | 预览扫描件 PDF | 点击预览 | 原生渲染原始页面，附"扫描件无文本层"提示 |

### 2.4 问答（P0）

| ID | 用例 | 步骤 | 预期 |
|----|------|------|------|
| TC-040 | 流式问答 | 上传文档后提问"什么是兰台？" | 答案**逐字流式**返回；来源以小字显示（文档名/相似度/预览链接） |
| TC-041 | 来源预览 | 点击来源"预览" | 打开该文档预览浮层 |
| TC-042 | 相似度分数 | 查看来源 | 显示 0~1 相似度分数 |
| TC-043 | 无命中提示 | 提问无关内容 | 答案提示"未检索到相关内容" |
| TC-044 | AI 不可用 | 停掉 Mock/Ollama 后提问 | 中文错误提示（503 本地服务未就绪），非英文堆栈 |
| TC-045 | 提问后清空 | 输入问题回车 | 输入框自动清空 |
| TC-046 | 历史上下文 | 同一会话连问两次 | 第二次回答参考第一次（Mock 回复可见携带历史） |

### 2.5 对话历史（P1）

| ID | 用例 | 步骤 | 预期 |
|----|------|------|------|
| TC-050 | 首轮自动标题 | 未选会话直接提问 | 会话列表出现新会话，标题=问题前 20 字（超长截断） |
| TC-051 | 新建对话 | 点击"＋ 新建对话" | 消息区清空，可开始新会话 |
| TC-052 | 切换会话 | 点击历史会话 | 恢复该会话历史消息 |
| TC-053 | 重命名 | 悬停会话→✎→输入新名 | 标题更新 |
| TC-054 | 删除 | 悬停会话→🗑→确认 | 会话及其消息删除 |
| TC-055 | 标题截断悬停 | 长标题会话 | 标题省略号截断，鼠标悬停显示全文 |

### 2.6 配置功能（P0）

| ID | 用例 | 步骤 | 预期 |
|----|------|------|------|
| TC-060 | 默认密码 | 输入 `Admin#123` | 进入配置（切换为大窗口，内部滚动） |
| TC-061 | 错误密码 | 输入错误密码 | 中文提示"配置密码错误"；连续 5 次后锁定 1 分钟 |
| TC-062 | 供应商预置 | AI 配置选择"通义" | 自动填充 URL 与推荐模型；embedding 槽位选 DeepSeek 提示"不支持向量化" |
| TC-063 | 测试连接 | 填 Mock 地址点"测试" | 卡片内显示模型清单（mock-chat/mock-embed），点击模型名自动填入 |
| TC-064 | 自动保存 | 修改模型名后点击其他输入框（失焦） | 顶部提示"✓ 已自动保存 HH:MM"；重新打开配置值保留 |
| TC-065 | 改密 | 修改密码流程 | 旧密码校验、≥8 位、改后旧密码失效需重新验证 |
| TC-066 | Token 生成 | 生成 token | 明文仅展示一次；列表显示前缀/时间 |
| TC-067 | Token 调用 | 用 token 调 `/api/chat`（Bearer） | 200 正常回答；吊销后 401 |
| TC-068 | 关于 | 查看关于 | 版本 0.1.17、平台、数据目录 |

### 2.7 外部 API（P1）

| ID | 用例 | 步骤 | 预期 |
|----|------|------|------|
| TC-070 | 非流式 chat | `POST /api/chat` | 返回 `{code:0,data:{answer,sources}}` |
| TC-071 | 流式 chat | `POST /api/chat/stream`（curl -N） | SSE 事件序列：sources → delta×N → done |
| TC-072 | 无效会话 | 流式/非流式带 `conversation_id:9999` | 均返回 404 中文"对话不存在" |
| TC-073 | 无效 token | Bearer 传错 token | 401 中文提示 |
| TC-074 | 参数校验 | question 为空 | 400/422 中文提示 |

### 2.8 智能体日志（P1）

| ID | 用例 | 步骤 | 预期 |
|----|------|------|------|
| TC-080 | 日志内容 | 问答/OCR 后查看 `agent-*.log` | JSON 行含 slot、messages（提示词全文）、reasoning（思维链）、answer、usage、duration_ms、ok |
| TC-081 | 图片占位 | 扫描件 OCR 后查看 | messages 中图片为"[图片数据 N 字节，已省略]"（无 base64） |

### 2.9 PDF 解析质量（P1，抽样）

| ID | 用例 | 步骤 | 预期 |
|----|------|------|------|
| TC-090 | 阅读顺序 | 上传 Asperger 样本，预览第 2 页 | 页眉页码碎片不混入正文，正文顺序连贯（对比 0.1.8 前：以"3 8 / Hans Asperger"开头） |
| TC-091 | 页级判定 | 上传混合 PDF（若有） | 图片页自动走 OCR，文本页正常提取 |
| TC-092 | 编码不可映射 PDF（0.1.16/0.1.17） | 上传 sample/GBT 43052 PDF（15 页，内嵌字体缺 ToUnicode） | 分类为**图片 PDF（OCR）**；预览 note 显示"检测到文本层但编码不可映射（内嵌字体缺 ToUnicode）…经 OCR 识别"；预览仍原生渲染 |
| TC-093 | 正常文字 PDF 不受影响 | 上传文字 PDF（如产品手册样例） | 分类为文字 PDF，可读性检测不误伤 |

---

## 三、验收标准

1. **P0 用例 100% 通过**（TC-001~TC-004、TC-020~TC-032、TC-040~TC-046、TC-060~TC-068 中标注 P0 者）；
2. P1 通过率 ≥ 95%（未通过项需记录缺陷并说明影响）；
3. 无功能完全不可用的严重缺陷；错误提示均为中文且含解决建议；
4. 智能体日志与应用日志正常生成，无异常堆栈刷屏。

## 四、测试记录模板

| 用例 ID | 测试人 | 日期 | 结果（通过/失败/阻塞） | 实际表现 | 缺陷编号 | 备注 |
|---------|--------|------|------------------------|----------|----------|------|
| TC-020 | | | | | | |

## 五、缺陷处理流程

1. 缺陷记录：在测试记录表中登记 + 描述复现步骤、实际/预期表现；
2. 修复入口：登记需求变更记录（CH 编号，类型=缺陷修复）→ 方案确认 → 实施（升版）→ 回归；
3. 回归范围：受影响模块的 P0 用例 + 该缺陷对应用例；
4. 测试完成产出：`docs/05-质量评审/测试报告.md`（含执行记录、通过率、缺陷清单、结论）。

---

**文档状态**: 测试方案 V1.1（针对应用 v0.1.17；V1.1：基线升 0.1.17，补 GBT 编码不可映射用例）
**生成时间**: 2026-08-23
**前置文档**: PRD产品需求文档.md、API说明文档.md
**后续建议**: 执行后产出测试报告.md
