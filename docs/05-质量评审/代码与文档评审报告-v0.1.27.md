# 兰台 v0.1.27 代码与文档综合评审报告

| 项目 | 内容 |
|------|------|
| 评审对象 | 兰台（lantai）本地 RAG 知识库 v0.1.27（含桌面壳 lantai-shell） |
| 评审范围 | backend/app/ 全部源码、frontend/、shell/（Tauri 2 壳工程）、scripts/、docs/ 全部文档；回归验证 v0.1.21 评审报告在 0.1.22 的整改（H1/H2/S-M1~S-M7/M3~M5/L1~L6）；评估 0.1.23~0.1.27 新增功能（终端标题版本、README+MIT、双通道会话、Token 复制降级、--host 参数化）实现质量 |
| 评审方式 | 逐文件阅读 + 跨模块交叉校验 + grep 精确核验；0.1.22 整改项逐条复核代码证据 |
| 生成时间 | 2026-08-24 |
| 结论 | v0.1.21 评审所列 2 高危 + 7 中危 + 文档滞后**全部在 0.1.22 落地验证到位**；0.1.23~0.1.27 新增功能实现规范。本次新发现 **0 项高危/严重、0 代码 bug**、3 项中危（均为文档同步）、6 项低危。质量趋势持续向好——继 v0.1.16 主代码、本轮含 shell 工程在内**全量 0 代码 bug**，自测规范与历史教训条款持续生效 |

---

## 一、v0.1.21 评审问题回归验证（0.1.22 整改，CH-042）

| 编号 | 问题 | 整改状态 | 证据 |
|------|------|:---:|------|
| H1 | server.rs recent 字节切片 panic | ✅ 已修复 | `server.rs:55-62` 新增 `truncate_recent`（floor_char_boundary）+ `RECENT_MAX/RECENT_KEEP` 常量；调用点 :496 改用该函数；单测 :824-838（多字节跨 8192 边界不 panic） |
| H2 | keep-alive 端口竞态 | ✅ 已修复 | `server.rs:719-735` handle_close 保持运行分支：先 term.kill()+drop 杀前台树 → 轮询 port_listening 等端口释放（5s 超时）→ kill_by_port 兜底 → 再 spawn detached |
| S-M1 | 代理前台会话不生效 | ✅ 已修复 | `server.rs:414` start_session 内 spawn ConPTY 前调 `apply_proxy_env(&settings)` |
| S-M3 | 失败扫描误报 | ✅ 已修复 | `server.rs:491-493` 扫描前读 `phase == Phase::Boot` 门控，Ready 后不再快速失败 |
| S-M4 | 白名单 localhost:* 过宽 | ✅ 已修复 | `lib.rs:89-96` on_navigation 动态读 `settings.port` 收窄；`capabilities/default.json:7` remote.urls 静态收窄到 8000 |
| S-M6 | iframe 改端口不刷新 | ✅ 已修复 | `shell.ts:122-123` `if (frame.src !== p.url) frame.src = p.url`（去掉 loaded 一次性标志） |
| S-M7 | 下载文件名未净化 | ✅ 已修复 | `download.rs:125-136` 新增 `sanitize_filename`（file_name + 剥离分隔符/..）；:103 调用；单测 :284-307（绝对路径/穿越/正常/点号） |
| M3 | 技术对接方案滞后 | ✅ 已修复 | 含 task_queue(0.1.18)/shell/--server/单轨(0.1.22) |
| M4 | API说明文档未补 parse/queued | ✅ 已修复 | V1.25 含 parse GET/PUT、queued、双通道 |
| M5 | 测试方案基线停 0.1.17 | ✅ 已修复 | V1.2 升 0.1.22，补 TC-100~116 |
| L1~L6 | requirements 注释/版本记录/R112 等 | ✅ 已修复 | requirements 注释升 v0.1.22 |

**回归结论**：0.1.22 整改彻底，2 高危 + 7 中危 + 文档全部落地，证据明确，注释标注修复编号。**shell 工程 2 项高危代码 bug 已清除**。

---

## 二、0.1.23~0.1.27 新增功能实现质量

| 版本 | 功能 | 实现评估 |
|------|------|----------|
| 0.1.23 | 终端标题栏版本标识 | ✅ `commands.rs:18` `env!("CARGO_PKG_VERSION")` 经 state 下发；`shell.ts:108-109` `panelTitle.textContent = 兰台终端 · v${p.version}`；编译期常量升版自动同步，实现规范 |
| 0.1.24 | README 引导完善 + MIT 许可证 | ✅ `LICENSE`（MIT 1064 字节）存在；README:139 文档导航含 LICENSE；首次使用三步含智能体配置明细；实现规范 |
| 0.1.25 | 双通道会话（壳内 cookie 丢失修复） | ✅ `settings.py:70` verify 返回 `{session: token}`；`:33-38` `_require_session` header 优先（`header_session or session`）；全部配置端点加 `x_session: Header(alias="X-Lantai-Session")`；`app.js:13-14` localStorage + X-Lantai-Session 头；:530 verify 后存 localStorage；实现规范，双通道兼容（壳走 header、浏览器直开走 cookie） |
| 0.1.26 | Token 复制降级 + 壳剪贴板权限 | ✅ `app.js:787` execCommand("copy") 降级兜底；`download.rs:156-184` PermissionRequestedEventHandler 自动允许 ClipboardRead(6)/ClipboardWrite(7) + MultipleAutomaticDownloads；实现规范（前端降级 + 壳权限双保险） |
| 0.1.27 | 监听地址 --host 参数化 | ✅ `run.py:30-34` `--host` 默认 `config.DEFAULT_HOST`(127.0.0.1)；:44 `uvicorn.run(host=args.host)`；README:78 注明 `lantai.exe --host 0.0.0.0` 用法与安全考虑；实现规范（默认不变，远程测试无需重打包） |

**结论：0.1.23~0.1.27 新增功能实现规范，0 代码 bug。**

---

## 三、本次新发现问题

### 🟠 高危 / 🔴 严重

**无**。本次未发现任何高危或严重问题——0.1.22 清除了 v0.1.21 报告的 shell 2 项高危，0.1.23~0.1.27 新增功能实现规范。**含 shell 工程在内全量 0 代码 bug**，连续良好状态。

---

### 🟡 中危（均为文档同步滞后）

**M1. 测试方案内部不一致：文档头 v0.1.22，但 TC-004/TC-068 断言"0.1.17"**

`测试方案.md:5` 文档头"测试对象 兰台 v0.1.22"，`:185` 页脚"V1.2（v0.1.22）"——基线已升 0.1.22 ✅。但 `:47` TC-004 断言"版本号显示 **0.1.17**"、`:111` TC-068 断言"版本 0.1.17"——**用例断言未随基线升级更新**，与文档头矛盾。

> 影响：按该方案执行 TC-004/TC-068 会校验到错误版本号（0.1.17 ≠ 实际 0.1.27），测试误判失败。属 0.1.22 升基线时漏改用例断言（头升了、TC 没升）。定中危（内部不一致导致测试无效）。

**M2. 测试方案基线停 0.1.22，未跟进 0.1.23~0.1.27**

测试方案 V1.2 基线 0.1.22，未覆盖：
- 0.1.23 终端标题栏版本标识
- 0.1.25 双通道会话（壳内 verify→localStorage→X-Lantai-Session→配置 API 全链路）
- 0.1.26 Token 复制降级（execCommand 兜底 + 壳剪贴板权限）
- 0.1.27 --host 参数化（0.0.0.0 监听）

> 影响：5 个版本新功能无测试用例覆盖。定中危。

**M3. 技术对接方案未同步 0.1.23~0.1.27**

`技术对接方案.md` grep：无 `双通道`/`X-Lantai`/`--host`/`clipboard`/`终端标题`/`MIT`/`0.1.2[3-7]` 匹配。0.1.22 已同步 task_queue/shell/单轨，但 0.1.23~0.1.27 新增内容未反映：
- 双通道会话机制（0.1.25，重要的鉴权架构变更）
- --host 监听地址参数化（0.1.27）
- Token 复制降级 + 壳剪贴板权限（0.1.26）
- 终端标题版本标识（0.1.23）
- MIT 许可证（0.1.24）

> 影响：核心方案文档滞后 5 版，双通道会话是鉴权架构变更尤应记录。定中危。

---

### 🟢 低危

**L1. `requirements.txt` 注释写 v0.1.22（实际 0.1.27）**
`requirements.txt:1` "后端运行时依赖（v0.1.22）"，滞后 5 版。重演 v0.1.21 报告 L1 问题。

**L2. 版本记录 release 目录表 7 个"当前版本"并存**
`版本记录.md` release 目录表：0.1.21/0.1.22/0.1.23/0.1.24/0.1.25/0.1.26/0.1.27 均标"当前版本"——应仅 0.1.27 为当前版本，其余改"保留可回滚"。重演 v0.1.5 N-L5 / v0.1.21 L3 问题（第 3 次重现）。

**L3. API说明文档未提 --host 监听地址参数**
`API说明文档.md` V1.25/0.1.27，含双通道/parse ✅，但未提 `--host` CLI 参数。--host 为 run.py 启动参数（非 API），API 文档可不覆盖；但技术对接方案/运行说明应提（README:78 已提 ✅）。低危。

**L4. 数据库设计文档版本号滞后**
0.1.18 后表结构未变（queued 为 status 枚举值变更，非表结构），数据库设计文档版本号跟进属可选。低危。

**L5. 前端 onclick 内联 + esc 不转义引号（v0.1.11 L6 延后项沿用）**
`app.js` 仍以 `esc(...).replace(/'/g,"\\'")` 构造内联 JS。历史延后项，沿用。

**L6. SSE 同步生成器占用线程池 worker（v0.1.11 L7 延后项沿用）**
`chat.py` chat_stream 同步 gen 占 worker。历史延后项，沿用。

---

### ℹ️ 提示（非问题）

- **0.1.22 整改彻底可验证**：H1/H2/S-M1~S-M7 每项都有代码证据 + 修复编号注释 + 单测（H1 truncate_recent、S-M7 sanitize_filename 均补单测），体现质量闭环。
- **双通道会话设计周到**：header 优先 + cookie 兼容，壳内跨站 iframe 与浏览器直开两种形态都覆盖；verify 返回 session token 供 localStorage 存储，解决 SameSite=Lax cookie 在 WebView2 第三方上下文被拒的问题。
- **Token 复制降级双保险**：前端 execCommand 兜底 + 壳 PermissionRequested 自动允许剪贴板权限，纵深防御。
- **--host 参数化克制**：默认 127.0.0.1 不变（安全），远程测试显式传 0.0.0.0，无需重打包；README 注明安全考虑。
- **终端标题版本标识零维护**：CARGO_PKG_VERSION 编译期常量，升版自动同步，无手动更新负担。
- **MIT 许可证 + GitHub 公开**：LICENSE 存在，README 文档导航含 LICENSE，项目已推送 GitHub（https://github.com/awardat/lantai）。
- **版本号主同步 ✅**：config.py（0.1.27）、README（0.1.27）、API说明文档（V1.25/0.1.27）、版本记录（0.1.27）、shell 三处（Cargo.toml/tauri.conf.json/package.json 应均为 0.1.27——本轮未逐一核验 shell 三处，建议发布前核对）；**唯 requirements.txt 注释滞后**（L1）。

---

## 四、优点

1. **0.1.22 整改彻底**：2 高危 + 7 中危全部落地，shell 工程 panic/竞态 bug 清除，单测补齐（truncate_recent、sanitize_filename），质量闭环完整。
2. **双通道会话架构周到**：header 优先 + cookie 兼容，解决 WebView2 跨站 iframe cookie 限制，两种使用形态都覆盖，是壳内嵌 Web 应用鉴权的成熟方案。
3. **Token 复制降级双保险**：前端降级链 + 壳权限自动允许，纵深防御。
4. **--host 参数化设计克制**：默认安全（127.0.0.1），远程测试显式开启，无需重打包。
5. **终端标题版本零维护**：编译期常量自动同步，无手动更新负担。
6. **变更管理持续规范**：CH-042~CH-049 + 版本记录 0.1.22~0.1.27 双文档；CH-049 发布需确认条款制度化。
7. **自测规范持续生效**：含 shell 在内全量 0 代码 bug 印证规范有效；历史教训条款（H1/H2/H3/CH-039）防止重蹈覆辙。

---

## 五、修复优先级建议

| 优先级 | 编号 | 建议动作 |
|--------|------|----------|
| 下版前 | M1 | 测试方案 TC-004/TC-068 断言"0.1.17"改为"0.1.27"（与文档头一致） |
| 下版前 | M2 | 测试方案基线升 0.1.27，补终端标题/双通道/clipboard/--host 用例 |
| 下版前 | M3 | 技术对接方案同步 0.1.23~0.1.27（双通道会话/--host/clipboard/终端标题/MIT） |
| 顺手修 | L1 | requirements.txt 注释 v0.1.22 → v0.1.27 |
| 顺手修 | L2 | 版本记录 release 表去多余"当前版本"，仅 0.1.27 标当前 |
| 可延后 | L3~L6 | API 文档 --host、数据库文档版本号、onclick 纵深防御、SSE 线程池 |

> 按 AGENTS.md 版本规则，文档变更需升版本号至 **0.1.28**（第三段 +1）。本次无代码 bug，全部为文档同步滞后项。
> **建议**：文档同步滞后是 recurring 问题（每轮新版本窗口都重现），根源是"升版本时只改部分文档"。建议在自测规范「每轮变更必测清单」中**强化文档同步检查**：升版本时 config/README/requirements/API说明/技术对接/测试方案/版本记录**逐项核对**，避免下次再滞后。

---

## 六、质量趋势

| 版本 | 严重 | 高 | 中 | 低 | 代码 bug | 备注 |
|------|:---:|:---:|:---:|:---:|:---:|------|
| v0.1.1 | 1 | 1 | 3 | 9 | 是（S1） | 首次评审 |
| v0.1.5 | 0 | 0 | 3 | 12 | 否 | |
| v0.1.11 | 0 | 3 | 2 | 12 | 是（H1 NameError） | |
| v0.1.16 | 0 | 0 | 2 | 9 | 否 | 主代码 0 bug |
| v0.1.21 | 0 | 2 | 10 | 18 | 是（shell H1/H2） | shell 新技术栈引入 bug |
| v0.1.27 | **0** | **0** | **3** | **6** | **否** | **含 shell 全量 0 bug** |

**趋势持续向好**：v0.1.21 shell 高危在 0.1.22 清除后，v0.1.27 含 shell 在内全量 0 代码 bug。剩余问题全部为文档同步滞后（recurring，建议强化必测清单根治）。问题总数从 v0.1.1 的 14 项降至 v0.1.27 的 9 项，且严重度全部降为低/中危。

---

**文档状态**: 评审报告 v1.0（针对应用 v0.1.27，含桌面壳 lantai-shell）
**生成时间**: 2026-08-24
**前置报告**: docs/05-质量评审/代码与文档评审报告-v0.1.1.md、v0.1.5.md、v0.1.11.md、v0.1.16.md、v0.1.21.md
**说明**: 本报告仅评审，未修改任何被评审文件。0.1.22 整改项逐条复核代码证据确认落地。
