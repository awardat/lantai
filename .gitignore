# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# 运行时数据（rag.db、uploads/）
backend/data/

# 开发自测样例输出
scripts/sample_docs/

# 发布产物
release/
dist/
build-work-*/

# 构建配置（CH-058/M7：build/ 不再整体排除——此前吞掉了 !scripts/build/lantai.spec 否定规则
# 导致 spec 从未入库；现只排除 scripts/build/build-work-* 工作目录，spec 正常入库）
scripts/build/build-work-*/
*.spec
!scripts/build/lantai.spec

# 临时与系统
.DS_Store
Thumbs.db
.tmp*/

# 用户样本素材（分析工具可引用，不入库）
sample/

# 独立知识备忘（与本项目无关，不随项目推送 GitHub）
knowledge/

# 壳便携运行时配置（防遗漏）
settings.json
